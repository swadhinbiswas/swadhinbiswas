---
// Vercel Analytics - Server-side only import
// These are loaded globally by Vercel at runtime
---

{/* Vercel Analytics is automatically injected by Vercel deployment */}
{/* No need to manually import in Astro components */}
