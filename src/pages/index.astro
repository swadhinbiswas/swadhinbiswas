---
export const prerender = false;

import BaseLayout from "../layouts/BaseLayout.astro";
import { getDynamicConfig } from "../lib/config";
import JsonLd from "../components/seo/JsonLd.astro";

import Hero from "../components/home/Hero.astro";
import Impact from "../components/home/Impact.astro";
import Summary from "../components/home/Summary.astro";
import Timeline from "../components/home/Timeline.astro";
import Experience from "../components/home/Experience.astro";
import Skills from "../components/home/Skills.astro";
import FeaturedProjects from "../components/home/FeaturedProjects.astro";
import Research from "../components/home/Research.astro";
import OpenSource from "../components/home/OpenSource.astro";
import Writing from "../components/home/Writing.astro";
import Contact from "../components/home/Contact.astro";

const siteConfig = await getDynamicConfig();
const siteUrl = (siteConfig.url || Astro.url.origin).replace(/\/$/, "");
const author = siteConfig.author || "Portfolio";
const shortBio =
  siteConfig.bio?.short?.trim() ||
  "Senior Data Engineer & MLOps Engineer. Open to global opportunities.";

const locationText = siteConfig.location || "";
const [cityPart, ...countryParts] = locationText
  .split(",")
  .map((p) => p.trim())
  .filter(Boolean);

const sameAs = [
  siteConfig.links?.github,
  siteConfig.links?.linkedin,
  siteConfig.links?.twitter,
  ...(siteConfig.socials || []).map((s) => s.url),
  "https://scholar.google.com/citations?hl=en&user=xHqlwTAAAAAJ",
].filter((u, i, arr): u is string => Boolean(u) && arr.indexOf(u) === i);

const schema = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebSite",
      "@id": `${siteUrl}/#website`,
      url: `${siteUrl}/`,
      name: author,
      description: shortBio,
      inLanguage: "en-US",
    },
    {
      "@type": "Person",
      "@id": `${siteUrl}/#person`,
      name: author,
      url: `${siteUrl}/`,
      image: `${siteUrl}/og-image.png`,
      description: shortBio,
      jobTitle: "Senior Data Engineer & MLOps Engineer",
      sameAs,
      address: cityPart
        ? { "@type": "PostalAddress", addressLocality: cityPart, addressCountry: countryParts.join(", ") || "" }
        : undefined,
      email: siteConfig.email,
      knowsAbout: siteConfig.skills || [],
    },
  ],
};
---

<BaseLayout
  title={undefined}
  description={shortBio}
  ogType="profile"
>
  <JsonLd slot="head" schema={schema} />

  <Hero />
  <Impact />
  <Summary />
  <Timeline />
  <Experience />
  <Skills />
  <FeaturedProjects />
  <Research />
  <OpenSource />
  <Writing />
  <Contact />
</BaseLayout>
